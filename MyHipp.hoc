strdef codestr
print "---------------------"
proc abc() {
  seed = $1
  param = $2  
  codestr = "_140818_"
  {printf("from MyHipp.hoc : ------seed is = %d ---------sm = %d (Hz)\tcodestr = %s\n", seed, param, codestr)}
  {load_file("Hipp.hoc")}
}